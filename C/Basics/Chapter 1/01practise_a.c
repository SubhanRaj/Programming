#include <stdio.h> 
// This programm calculates area of rectange from Pre defined length & Breadth

int main(){
    printf("\n");
    float length = 12, breadth = 12;
    float area = length*breadth;

    printf ("The area for the rectangle is %f \n", area);


    return 0;
}