#include <stdio.h>
// Infinite loop

int main()
{
    int a;
    scanf("%d", &a);
    printf("\n");

    // while (a <= 25)
    a = 11;
    while (a > 10)
    {
        printf("%d\n", a);
        a++;
    }

    return 0;
}