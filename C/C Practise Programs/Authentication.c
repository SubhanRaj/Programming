// A Complex authentication program in C Language

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int name;     // name
    int age;      // Age of user
    int income;   //Annual income of user
    int spouse;   //Maritial status
    int child;    // parenting status of user
    int dlapply;  //Appling for driving licence
    int dlown;    //have driving licence
    int passport; //passport of user
    int cone;
    int ctwo;
    int cthree;
    int cfour;
    int cfive;
    int csix;
    int cseven; // choices

    printf("\n");
    printf("Hi dear, what's your name?");
    scanf("%d", &name);

    printf("Welcome to Regulation Authority %d \n \n", name);

    return 0;
}