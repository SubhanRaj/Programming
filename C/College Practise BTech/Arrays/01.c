#include <stdio.h>

int main()
{
    int a[10], i;
    printf("enter elements of array:\n");
    for (i = 0; i < 10; i++)
        scanf("%d", &a[i]);
    printf("\narray elements are %d", a[i]);
    for (i = 0; i < 10; i++)
        

    return 0;
}